<%- include ../head%>
<%promotions.forEach(promo => {%>
    <%=promo.code%> <br>
    <%=promo.amount%> <%=promo.isPercentage?"%":"$"%>
    <%=promo.expire%>
<%})%>
<%if(locals.user && user.id == event.userId){%>
    <a href="/event/<%=event.id%>/promotion/new%>"><button>Add Promotion</button></a>
<%}%>
<%-include ../foot%>
